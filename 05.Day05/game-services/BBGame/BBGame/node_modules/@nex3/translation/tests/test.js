const Translation = require("../");

const trans = Translation.getInstance("th", {
  testlang: "this is testing th",
  minBet: "The minimun stake of this hand is %{min} test th",
});

console.log(trans.t(trans.keys.betOnNextRound));
console.log(trans.t(trans.keys.testlang));
console.log(trans.t(trans.keys.minBet));
