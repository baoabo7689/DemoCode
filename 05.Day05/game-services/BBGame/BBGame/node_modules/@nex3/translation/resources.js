const loadDefaultResource = (language) => {
  let resource = {};
  try {
    resource = require(`./data/${language}.js`);
  } catch (e) {}

  return resource;
};

module.exports = {
  loadDefaultResource,
};
