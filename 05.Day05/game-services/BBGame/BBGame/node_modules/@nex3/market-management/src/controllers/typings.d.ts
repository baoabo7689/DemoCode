declare module Controller {
  import { GameConfigs } from "@nex3/market-management";

  interface MarketOptions {
    useCache?: boolean;
    cacheTimeout?: number;
    autoFetchMarketConfigs: boolean;
  }

  interface Market {
    getGameConfigs(gameId: number, currency: string, language: string): Promise<GameConfigs>;
  }
}
